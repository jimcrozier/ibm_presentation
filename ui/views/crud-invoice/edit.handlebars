{{#extend 'styles'}}
  <style>
    .error {
      color: red;
    }
    .success {
      color: green;
    }
  </style>
{{/extend}}
<!-- Content Header (Page header) -->
<section class="content-header">
  <h1>
    Edit Invoice
    <small>Edit Invoice info</small>
  </h1>
  <ol class="breadcrumb">
    <li><a href="{{{url 'dashboard.v1'}}}"><i class="fa fa-dashboard"></i> Home</a></li>
    <li><a href="{{{url 'invoice.list'}}}">List</a></li>
    <li class="active">Edit Invoice</li>
  </ol>
</section>
<!-- Main content -->
<section class="content">
  <div class="row">
    <div class="col-md-6">
      {{#if message}}
        {{{message}}}
      {{/if}}
      <div class="box box-info">
        <div class="box-header with-border">
          <h3 class="box-title">Invoice Info</h3>
        </div>
        <!-- form -->
        <form action="/crud/invoice/{{crud_invoice.id}}?_method=put" method="post">
          <div class="box-body">
            <div class="form-group has-feedback">
              <input type="text" class="form-control" name="invoice[displayName]" placeholder="Display Name" value="{{crud_invoice.displayName}}" required/>
              <span class="glyphicon glyphicon-invoice form-control-feedback"></span>
            </div>
            <div class="form-group has-feedback">
              <input type="text" class="form-control" name="invoice[invoicename]" placeholder="Invoicename" value="{{crud_invoice.invoicename}}" readonly="true" />
              <span class="glyphicon glyphicon-invoice form-control-feedback"></span>
            </div>
          </div>
          <!-- /.box-body -->
          <div class="box-footer">
            <button type="submit" class="btn btn-primary">Update</button>
            <button type="button" class="btn btn-default" onclick="location.href = '{{{url "invoice.list"}}}'">Cancel</button>
          </div>
          <!-- /.box-footer -->
        </form>
        <!-- /form -->
      </div>
      <!-- /.box -->
    </div>
    <!-- /.col-md-6 -->
  </div>
  <!-- /.row -->
</section>
<!-- /.section -->